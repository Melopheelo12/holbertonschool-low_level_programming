#include "main.h"
/**
 *print_rev -Affiche le sens inverse
 *
 *
 *
 *
 */
void print_rev(char *s)
{
int len = 0;
for (; s[len] != '\0'; len++)
;
for (int z = len - 1; i >= 0; i--)
_putchar(s[i]);
_putchar('\n');
}
